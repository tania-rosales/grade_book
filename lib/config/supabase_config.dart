class SupabaseConfig {
  // ─── Reemplaza estos valores con los de TU proyecto ───
  // Los encuentras en: Supabase Dashboard → Settings → API
  //
  // Project URL: es la dirección de tu proyecto en la nube
  // Anon Key: es la llave pública para acceder a la API
 
  static const String url = 'example';
  static const String anonKey = 'example';
}